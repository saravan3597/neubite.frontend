---
description: Creates a temporary mock for a backend endpoint
---

**Instructions to Agent:** "Create a temporary mock function that returns a Promise resolving to dummy data matching the TypeScript interface. Add a `setTimeout` to simulate network latency."