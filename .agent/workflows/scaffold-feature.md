---
description: Generates a complete React feature module
---

**Instructions to Agent:** "Ask the user for the feature name. Then create a folder in `src/features/[name]` containing an `index.ts`, a main component file, a `.test.tsx` file, and a dedicated Zustand store if state is required."